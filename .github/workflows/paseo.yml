name: paseo
concurrency:
  group: ${{ github.workflow }}-${{ inputs.runner }}
  cancel-in-progress: true
on:
  workflow_dispatch:
    inputs:
      runner:
        description: runner
        required: true
        default: ubuntu-latest
        type: choice
        options:
          - ubuntu-latest
          - ubuntu-26.04-arm
          - macos-latest
      pair-link:
        description: 'Pairing link'
        required: false
        default: false
        type: boolean
jobs:
  paseo:
    timeout-minutes: 315
    runs-on: ${{ inputs.runner }}
    steps:
      - uses: actions/checkout@main
        with:
          path: ${{ github.event.repository.name }}
          fetch-depth: "0"
          token: ${{ secrets.GH_PAT }}
      - uses: ./kudzu/.github/actions/install-nix
      - uses: actions/checkout@main
        with:
          repository: ${{ github.actor }}/nixpkgs
          fetch-depth: "2"
          path: nixpkgs
          token: ${{ secrets.GH_PAT }}
      - uses: actions/checkout@main
        with:
          repository: ${{ github.actor }}/torvox
          fetch-depth: "0"
          path: torvox
          token: ${{ secrets.GH_PAT }}
      - uses: cachix/cachix-action@master
        timeout-minutes: 2
        continue-on-error: true
        with:
          name: ${{ secrets.CACHIX_NAME }}
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - uses: actions/cache/restore@main
        id: cache
        with:
          path: |
            ~/.paseo
            ~/.pi/agent
            ~/.local/share/cortexkit/magic-context
            ~/.reasonix
          key: ${{ github.workflow }}-${{ runner.os }}-${{ runner.arch }}-${{ github.run_id }}
          restore-keys: |
            ${{ github.workflow }}-${{ runner.os }}-${{ runner.arch }}-
      - working-directory: ${{ github.event.repository.name }}
        run: |
          set -euo pipefail
          rm -f "$HOME/.config/nix/nix.conf"
          mkdir -p ~/.config/git
          nix build --verbose --print-build-logs --print-out-paths --impure '.#nixosConfigurations.default.config.home-manager.users.nix-on-droid.xdg.configFile."git/ignore".source' --out-link /tmp/git-ignore && install -m 0644 /tmp/git-ignore ~/.config/git/ignore && rm -f /tmp/git-ignore
          mkdir -p "$HOME/.paseo" "$HOME/.pi/agent/skills" "$HOME/.pi/agent/extensions" "$HOME/.pi/agent/sessions" "$HOME/.config/mcp" "$HOME/.config/cortexkit" "$HOME/.local/share/cortexkit/magic-context" "$HOME/.reasonix"
          nix profile add nixpkgs#openspec nixpkgs#rust-analyzer nixpkgs#ktlint nixpkgs#ruff nixpkgs#bun nixpkgs#skills .#reasonix
          npm install --global --yes @getpaseo/cli
          install -m 0644 .github/paseo-config.json ~/.paseo/config.json
          bun add -g --ignore-scripts @earendil-works/pi-coding-agent
          nix build --verbose --print-build-logs --print-out-paths --impure .#pi-agent-settings --out-link /tmp/pi-agent-settings && install -m 0644 /tmp/pi-agent-settings ~/.pi/agent/settings.json && rm -f /tmp/pi-agent-settings
          nix build --verbose --print-build-logs --print-out-paths --impure .#pi-agent-mcp --out-link /tmp/pi-agent-mcp && install -m 0644 /tmp/pi-agent-mcp ~/.config/mcp/mcp.json && rm -f /tmp/pi-agent-mcp
          cp --recursive .github/pi-opencode ~/.pi/agent/extensions/pi-opencode
          pushd ~/.pi/agent/extensions/pi-opencode && npm install --yes && popd
          install -m 0644 .github/AGENTS.md ~/.pi/agent/AGENTS.md
          cp .github/magic-context.jsonc ~/.config/cortexkit/magic-context.jsonc
          npm install --global --yes open-websearch
          npm install -g @tobilu/qmd
          nix build --verbose --print-build-logs --print-out-paths --impure .#reasonix-config --out-link /tmp/reasonix-config && install -m 0644 /tmp/reasonix-config ~/.reasonix/config.toml && rm -f /tmp/reasonix-config
          install -m 0644 .github/AGENTS.md ~/.reasonix/AGENTS.md
      - run: |
          cat << 'EOF' > ~/.pi/agent/auth.json
          {
            "opencode": {
              "type": "api_key",
              "key": "public"
            },
            "nvidia": {
              "type": "api_key",
              "key": "${{ secrets.NVIDIA_API_KEY }}"
            }
          }
          EOF
      - run: |
          echo "NVIDIA_API_KEY=${{ secrets.NVIDIA_API_KEY }}" > ~/.reasonix/.env
          echo "OPENCODE_API_KEY=public" >> ~/.reasonix/.env
      - run: echo "${{ secrets.GH_PAT }}" | gh auth login --with-token
      - working-directory: ${{ github.event.repository.name }}
        env:
          PI_SKIP_VERSION_CHECK: "1"
          PI_TELEMETRY: "0"
        run: |
          set -euo pipefail
          skills add mattpocock/skills --agent pi --global --yes
          skills add addyosmani/agent-skills --agent pi --global --yes
          skills add Fission-AI/OpenSpec --agent pi --global --yes
          skills add blader/humanizer --agent pi --global --yes
          git clone --depth 1 --single-branch https://github.com/aldefy/compose-skill.git /tmp/compose-skill && cp -r /tmp/compose-skill/skills/compose-expert $HOME/.pi/agent/skills/ && rm -rf /tmp/compose-skill
          skills add https://github.com/Aas-ee/open-webSearch --skill open-websearch --agent pi --global --yes
          skills add getpaseo/paseo --agent pi --global --yes
          skills add levi-qiao/longgraph-skill --full-depth --skill '*' --agent pi --global --yes
          export BUN_INSTALL="$HOME/.bun"
          export PATH="$BUN_INSTALL/bin:${ANDROID_HOME:+$ANDROID_HOME/platform-tools:}$PATH"
          pushd .github/reasonix-config
          pip install -e .
          reasonix-config
          popd
          pi --help
          if [[ '${{ inputs.pair-link }}' == 'true' ]]; then
            paseo onboard --relay --voice disable
          else
            paseo onboard --relay --voice disable > /dev/null
          fi
          tail -f "$HOME/.paseo/daemon.log"
      - if: always()
        run: |
          paseo daemon stop
          rm -rf ~/.reasonix/logs ~/.reasonix/config.toml* ~/.reasonix/.env ~/.reasonix/skills ~/.reasonix/mcp-state ~/.reasonix/commands ~/.reasonix/AGENTS.md ~/.paseo/paseo.pid ~/.paseo/daemon.log ~/.pi/agent/AGENTS.md ~/.pi/agent/models-store.json ~/.pi/agent/settings.json ~/.pi/agent/auth.json ~/.pi/agent/mcp-cache.json ~/.pi/agent/npm ~/.pi/agent/skills ~/.pi/agent/extensions
      - if: always()
        uses: actions/cache/save@main
        with:
          path: |
            ~/.paseo
            ~/.pi/agent
            ~/.local/share/cortexkit/magic-context
            ~/.reasonix
          key: ${{ github.workflow }}-${{ runner.os }}-${{ runner.arch }}-${{ github.run_id }}
