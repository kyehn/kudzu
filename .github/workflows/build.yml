name: build
on:
  workflow_dispatch:
    inputs:
      package:
        description: package name
        required: false
        default: ""
        type: string
      command:
        description: command
        required: false
        default: ""
        type: string
      runner:
        description: runner
        required: true
        default: ubuntu-26.04-arm
        type: choice
        options:
          - ubuntu-latest
          - ubuntu-26.04-arm
          - macos-latest
jobs:
  build:
    runs-on: ${{ inputs.runner }}
    steps:
      - uses: actions/checkout@main
      - uses: ./.github/actions/install-nix
      - uses: cachix/cachix-action@master
        timeout-minutes: 1
        continue-on-error: true
        with:
          name: ${{ secrets.CACHIX_NAME }}
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - run: |
          rm -f ~/.config/nix/nix.conf
          nix flake update
      - if: ${{ inputs.package != '' }}
        run: |
          nix build --verbose --print-build-logs --print-out-paths --impure .#${{ inputs.package }}
      - if: ${{ inputs.command != '' }}
        run: ${{ inputs.command }}
        # nix build --verbose --print-build-logs --print-out-paths --impure github:NixOS/nixpkgs/master#librewolf
