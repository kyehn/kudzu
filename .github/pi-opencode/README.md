# pi-opencode

- 参考 `overlays/reasonix/README.md` 中对 opencode 模拟的要求
- 只显示免费的 opencode 模型
- 只有一个 `opencode` provider，没有重复，没有 `opencode-patched` 或类似 providers
- 使用 `ProviderModelConfig` `ProviderConfig` 等数据
